# 程式清單

最後更新時間：2026/06/03 14:24:20

## 1. 版本記錄

| 版本 | 時間 | 更新內容 | 維護者 |
|---|---|---|---|
| 1.0 | 2026/05/23 01:19:00 | 建立 Vue3 專案基礎、登入頁與路由 | Codex |
| 1.1 | 2026/05/23 02:41:00 | 新增 Express mock API 與 JSON 資料檔 | Codex |
| 1.2 | 2026/05/23 11:57:00 | 新增貸款資料更新與單元測試 | Codex |
| 1.3 | 2026/05/23 13:42:01 | 新增貸款異動紀錄與系統文件 | Codex |
| 1.4 | 2026/05/24 02:16:46 | 新增 `.gitignore` 與 GitHub 版控流程 | Codex |
| 1.5 | 2026/05/24 02:18:52 | 統一核心文件為乾淨 UTF-8 中文內容，並將 mock 帳密與資料改為安全範例 | Codex |
| 1.6 | 2026/05/24 02:29:19 | 補充 README GitHub SSH Key、host key、推送錯誤處理與完整上傳流程 | Codex |
| 1.7 | 2026/05/24 02:40:18 | 註冊頁新增使用者名稱欄位與長度檢核，註冊 API 改為寫入前端輸入的使用者名稱 | Codex |
| 1.8 | 2026/05/24 02:53:00 | 完成註冊頁實際網頁操作驗證，確認短使用者名稱提示與註冊後首頁顯示使用者名稱 | Codex |
| 1.9 | 2026/05/24 03:17:13 | 導入 Vuex、props/emit 共用元件、RouterLink、onMounted、vee-validate、vue-next-select，並同步更新單元測試 | Codex |
| 2.0 | 2026/05/24 20:16:14 | 強化放款資訊更新頁驗證：發票號碼限制 13 碼數字、不得重複，且當前現欠金額需大於等於下期還款金額；新增 Vue3 技術使用欄位矩陣 | Codex |
| 2.1 | 2026/05/24 20:40:19 | 修正放款資訊更新頁幣別下拉選單被表格裁切、可點擊範圍過小的問題，補強 vue-next-select 下拉高度、z-index 與選項點擊區域 | Codex |
| 2.2 | 2026/05/24 20:59:57 | 首頁與放款資訊更新頁新增總現欠金額、總還款金額與目標幣別折算；新增匯率 mock API、reactive 總額狀態與安全範例測試資料修正 | Codex |
| 2.3 | 2026/05/24 21:21:13 | 新增當前匯率資訊頁，支援從總額區帶入本位幣、切換本位幣、顯示匯率更新時間與各幣別比值表格 | Codex |
| 2.4 | 2026/05/24 22:01:44 | 修正「當前匯率資訊」入口為另開新視窗或新分頁，方便放款頁與匯率頁同步比對 | Codex |
| 2.5 | 2026/05/25 01:28:46 | 修正新視窗開啟當前匯率資訊頁時被路由守衛導走的問題，匯率資訊頁改為可直接開啟 | Codex |
| 2.6 | 2026/05/25 10:07:47 | 新增 API 連線模式參數與獨立後端 API 設定檔，可用 FLAG 切換 mock API 或實際後端 API | Codex |
| 2.7 | 2026/05/25 10:23:50 | 修正後端 API 模式未設定 URL 時 fallback mock 的問題，改為直接回報設定錯誤 | Codex |
| 2.8 | 2026/05/25 10:29:18 | 建立本機 `.env` 連接 `http://127.0.0.1:8080` 實際後端 API，完成端點與前端畫面驗證 | Codex |
| 2.9 | 2026/05/25 10:40:09 | 新增後台 API 狀態檢查頁 `/admin/api-health`，列出完整 URL、中文名稱與當前狀態 | Codex |
| 3.0 | 2026/05/25 10:59:33 | 補充實際後端 API 測試排查說明，明確區分 `npm run dev` 與 `npm run dev:full` 適用情境，避免多個 Vite/mock 行程造成誤判 | Codex |
| 3.1 | 2026/05/25 11:01:00 | 修正實際後端 API 排查說明，明確指出 mock API 為 3001、後端 API 為 8080，根因應優先聚焦多個 Vite 前端行程或舊環境設定 | Codex |
| 3.2 | 2026/05/25 11:10:52 | 強化 `/admin/api-health` 診斷資訊，新增 API 模式、Base URL、檢查時間與錯誤明細，並重啟單一 Vite 行程完成瀏覽器實測 | Codex |
| 3.3 | 2026/05/25 11:35:00 | 將 Vue3 核心前端程式、SFC script 與測試統一遷移為 TypeScript；mock server 因非 Vue3 核心專案保留 JavaScript | Codex |
| 3.4 | 2026/05/25 19:47:07 | 網頁名稱改為 `VUE3發票登入平台`，mock 模式加註 `(mock)`；所有發票號碼相關中文顯示與驗證訊息同步調整 | Codex |
| 3.5 | 2026/05/29 22:14:50 | 建立 API 自建模組三層架構：API 設定層、API Client 層、業務 API 層；補強主要函式 TypeScript 型別並同步測試 | Codex |
| 3.6 | 2026/06/03 14:24:20 | 新增 `VUE3專案文件SOP.md`，整理 Vue3 專案建立、API 分層、mock 安全資料、測試、文件維護與版控檢查流程 | Codex |

## 2. 程式碼路徑

專案根目錄：

```txt
C:\Users\tommy\Desktop\vue3-loan-project
```

## 3. 分類項目與檔案用途

### 3.1 專案設定與文件

| 路徑 | 檔案名稱 | 檔案用途 |
|---|---|---|
| 專案根目錄 | `package.json` | 定義 npm scripts、dependencies、devDependencies。 |
| 專案根目錄 | `package-lock.json` | 鎖定 npm 套件版本，確保環境一致。 |
| 專案根目錄 | `vite.config.ts` | 設定 Vite、Vue plugin、Vitest 測試環境與 TypeScript 設定載入。 |
| 專案根目錄 | `tsconfig.json` | TypeScript 編譯與 `vue-tsc` 型別檢查設定。 |
| 專案根目錄 | `env.d.ts` | Vue SFC、Vite 與 Vuex 型別宣告補充。 |
| 專案根目錄 | `.env.example` | 提供 API 連線模式、mock API URL、實際後端 API URL 與 timeout 範例設定。 |
| 專案根目錄 | `.gitignore` | 排除 `node_modules/`、`dist/`、環境檔與暫存產物。 |
| 專案根目錄 | `README.md` | 專案說明、啟動方式、測試方式與 GitHub 版控流程。 |
| 專案根目錄 | `AGENTS.md` | 專案維護規範與文件同步要求。 |
| 專案根目錄 | `前端系統分析設計規格書.md` | 業務需求、流程、API、驗收測試與圖表。 |
| 專案根目錄 | `VUE3專案文件SOP.md` | Vue3 專案建立、功能擴充、API 分層、測試、文件維護與版控檢查的標準作業流程。 |

### 3.2 前端主程式

| 路徑 | 檔案名稱 | 檔案用途 |
|---|---|---|
| `src` | `main.ts` | 建立 Vue app，掛載 router 與全域 CSS。 |
| `src` | `App.vue` | Vue 根元件，顯示目前路由頁面。 |
| `src/api/config` | `backendApiConfig.ts` | API 設定層，集中管理 mock/backend 模式、base URL、timeout、各 API method、URL 與健康檢查設定。 |
| `src/api/client` | `httpClient.ts` | API Client 層，建立共用 Axios instance，統一 baseURL、headers 與 timeout。 |
| `src/api/client` | `request.ts` | API Client 層，依端點名稱發送 request，統一錯誤轉換與 `success` 欄位檢查。 |
| `src/api/modules` | `authApi.ts` | 業務 API 層，封裝登入與註冊 API。 |
| `src/api/modules` | `userApi.ts` | 業務 API 層，封裝使用者資料查詢 API。 |
| `src/api/modules` | `loanApi.ts` | 業務 API 層，封裝發票放款資料更新與異動紀錄查詢 API。 |
| `src/api/modules` | `exchangeRateApi.ts` | 業務 API 層，封裝匯率查詢 API。 |
| `src/api/modules` | `apiHealthApi.ts` | 業務 API 層，封裝後台 API 狀態檢查清單、執行環境資訊與安全端點探測。 |
| `src/api/modules` | `normalizers.ts` | 業務 API 層，集中正規化使用者、發票放款與異動紀錄 response。 |
| `src/api` | `index.ts` | API 自建模組匯出入口，集中匯出設定、Client 與業務 API。 |
| `src/config` | `backendApiConfig.ts` | 相容舊引用路徑的 re-export；新功能應改用 `src/api/config/backendApiConfig.ts`。 |
| `src/router` | `index.ts` | 設定登入、首頁、註冊、貸款更新頁、匯率資訊頁路由與登入檢查。 |
| `src/stores` | `sessionStore.ts` | 儲存目前登入使用者 session 狀態。 |
| `src/services` | `authApi.ts` | 相容舊引用路徑的 re-export；新功能應改用 `src/api/modules/*`。 |
| `src/services` | `apiHealthService.ts` | 相容舊引用路徑的 re-export；新功能應改用 `src/api/modules/apiHealthApi.ts`。 |
| `src/styles` | `main.css` | 全域樣式與 RWD 排版。 |
| `src/utils` | `currencyTotals.ts` | 提供放款金額跨幣別折算、總額加總與金額格式化工具。 |
| `src/utils` | `pageTitle.ts` | 依 API 模式產生瀏覽器分頁標題，mock 模式加註 `(mock)` 避免環境誤判。 |
| `src/types` | `api.ts` | 定義 API 設定、健康檢查列與執行環境資訊型別。 |
| `src/types` | `loan.ts` | 定義使用者、放款、異動紀錄、匯率與總額相關型別。 |

### 3.3 頁面元件

| 路徑 | 檔案名稱 | 檔案用途 |
|---|---|---|
| `src/views` | `LoginView.vue` | 登入頁，處理帳號密碼輸入、驗證與登入 API。 |
| `src/views` | `RegisterView.vue` | 註冊頁，處理帳號、使用者名稱、密碼建立與註冊 API，並檢核使用者名稱至少 2 個字元。 |
| `src/views` | `HomeView.vue` | 會員首頁，顯示使用者資料、貸款資料與異動紀錄入口。 |
| `src/views` | `UpdateLoansView.vue` | 貸款資料維護頁，支援新增、刪除、修改與儲存貸款資料。 |
| `src/views` | `ExchangeRatesView.vue` | 當前匯率資訊頁，支援本位幣切換、匯率更新時間與各幣別比值表格。 |
| `src/views` | `AdminApiHealthView.vue` | 後台 API 狀態檢查頁，顯示 API 模式、Base URL、完整 URL、URL 中文名稱、當前狀態與錯誤明細。 |
| `src/components` | `LoanTotalsSummary.vue` | 放款總額摘要元件，顯示總現欠金額、總還款金額與目標幣別下拉選單。 |

### 3.4 Mock API 與資料

| 路徑 | 檔案名稱 | 檔案用途 |
|---|---|---|
| `server` | `mockApiServer.js` | Express mock API server，提供登入、註冊、查詢、貸款更新與匯率查詢 API，註冊時將前端輸入的使用者名稱寫入 profile JSON。 |
| `server` | `user-credentials.json` | 安全範例帳密資料。 |
| `server` | `user-profiles.json` | 安全範例使用者名稱資料。 |
| `server` | `user-loans.json` | 安全範例貸款資料。 |
| `server` | `user-loan-change-logs.json` | 安全範例貸款異動紀錄。 |

### 3.5 單元測試

| 路徑 | 檔案名稱 | 檔案用途 |
|---|---|---|
| `src/test` | `setup.ts` | Vitest 測試初始化，載入 Testing Library DOM matcher。 |
| `src/views` | `LoginView.test.ts` | 測試登入欄位驗證、登入成功與錯誤訊息。 |
| `src/views` | `RegisterView.test.ts` | 測試註冊欄位驗證、使用者名稱長度檢核、註冊成功與錯誤訊息。 |
| `src/views` | `HomeView.test.ts` | 測試首頁貸款資料、異動紀錄與放款總額顯示。 |
| `src/views` | `UpdateLoansView.test.ts` | 測試貸款新增、刪除、欄位驗證、放款總額與儲存流程。 |
| `src/views` | `ExchangeRatesView.test.ts` | 測試匯率資訊頁預設本位幣、網址帶入本位幣、更新時間與匯率比值表格。 |
| `src/views` | `AdminApiHealthView.test.ts` | 測試後台 API 狀態檢查頁執行環境資訊、欄位、狀態、重新檢查與避免異動資料 API。 |
| `src/router` | `index.test.ts` | 測試匯率資訊頁未套用登入守衛，確保新視窗可直接顯示匯率資訊。 |
| `src/config` | `backendApiConfig.test.ts` | 測試 API 連線模式預設值、後端模式切換、後端 URL 必填、method 與動態 URL 組裝。 |
| `src/utils` | `pageTitle.test.ts` | 測試正式與 mock 模式的瀏覽器分頁標題。 |

## 3.7 Vue3 技術使用欄位矩陣

| 分類項目 | 程式碼路徑 | 檔案名稱 | 檔案用途 | 使用 VUE3 技術 |
|---|---|---|---|---|
| 入口設定 | `src` | `main.ts` | 建立 Vue app，掛載 Vuex、Router、全域樣式與 vue-next-select 樣式。 | vuex、router-view 關聯入口 |
| 根元件 | `src` | `App.vue` | 顯示目前路由頁面。 | router-view |
| 路由設定 | `src/router` | `index.ts` | 管理登入、註冊、首頁、放款更新頁路由與登入守衛。 | router-link 關聯路由、router-view 關聯路由 |
| 狀態管理 | `src/store` | `index.ts` | 建立 Vuex store，集中保存登入後使用者資料。 | vuex |
| 狀態管理 | `src/stores` | `sessionStore.ts` | 保留既有 sessionStore 介面，內部改用 Vuex getter/commit。 | vuex |
| API 設定 | `src/api/config` | `backendApiConfig.ts` | API 設定層，以參數檔集中控制 mock/後端 API 切換、base URL、method、URL 與健康檢查設定。 | axios 關聯設定 |
| API Client | `src/api/client` | `httpClient.ts` | API Client 層，建立共用 Axios instance，統一 baseURL、headers 與 timeout。 | axios |
| API Client | `src/api/client` | `request.ts` | API Client 層，統一 request 執行、端點解析、錯誤轉換與 success 欄位檢查。 | axios |
| 業務 API | `src/api/modules` | `authApi.ts` | 業務 API 層，封裝登入與註冊。 | axios |
| 業務 API | `src/api/modules` | `userApi.ts` | 業務 API 層，封裝使用者資料查詢。 | axios |
| 業務 API | `src/api/modules` | `loanApi.ts` | 業務 API 層，封裝發票放款更新與異動紀錄查詢。 | axios |
| 業務 API | `src/api/modules` | `exchangeRateApi.ts` | 業務 API 層，封裝匯率查詢。 | axios |
| 業務 API | `src/api/modules` | `apiHealthApi.ts` | 業務 API 層，封裝後台 API 狀態檢查。 | axios |
| API 相容層 | `src/services` | `authApi.ts` | 舊 service 路徑 re-export，新功能改用 `src/api/modules/*`。 | axios 關聯 |
| API 相容層 | `src/services` | `apiHealthService.ts` | 舊 service 路徑 re-export，新功能改用 `src/api/modules/apiHealthApi.ts`。 | axios 關聯 |
| 共用元件 | `src/components` | `FormField.vue` | 表單輸入共用元件，父層以 v-model 綁定。 | props、emit、v-model、v-on、v-bind |
| 共用元件 | `src/components` | `LoanRowsEditor.vue` | 放款列編輯元件，處理發票號碼、幣別、金額、日期輸入。 | props、emit、v-for、vue-next-select、v-on、v-bind |
| 共用元件 | `src/components` | `LoanTotalsSummary.vue` | 放款總額摘要元件，父層以 v-model 綁定目標幣別，並提供另開新視窗或新分頁的當前匯率資訊頁入口。 | props、emit、router-link、vue-next-select、v-model、v-on、v-bind |
| 工具函式 | `src/utils` | `currencyTotals.ts` | 集中處理匯率折算、總額加總、匯率比值列與金額格式化。 | 純函式，供 computed 使用 |
| 頁面功能 | `src/views` | `LoginView.vue` | 帳密登入與註冊頁導覽。 | ref、router-link、v-model、v-on、v-bind |
| 頁面功能 | `src/views` | `RegisterView.vue` | 帳密註冊、使用者名稱輸入與表單驗證。 | ref、router-link、vee-validate、v-model、v-on、v-bind |
| 頁面功能 | `src/views` | `HomeView.vue` | 使用者資訊、放款資訊、折算後放款總額與異動紀錄顯示。 | reactive、computed、onMounted、router-link、v-for、v-model、v-on、v-bind |
| 頁面功能 | `src/views` | `UpdateLoansView.vue` | 放款資訊更新，含 13 碼帳號、不可重複、金額大小驗證與折算後放款總額。 | ref、reactive、computed、onMounted、props/emit 關聯、v-model、v-bind、v-on |
| 頁面功能 | `src/views` | `ExchangeRatesView.vue` | 當前匯率資訊頁，依本位幣計算各幣別匯率比值，並同步網址 query。 | reactive、computed、onMounted、watch、router-link、vue-next-select、v-for、v-model、v-bind、v-on |
| 頁面功能 | `src/views` | `AdminApiHealthView.vue` | 後台 API 狀態檢查頁，載入後自動檢查安全端點、顯示目前 API 模式與錯誤明細，並可手動重新檢查。 | ref、onMounted、v-for、v-bind、v-on |
| 單元測試 | `src/views` | `UpdateLoansView.test.ts` | 測試放款更新頁輸入限制、重複檢核、金額大小檢核與存檔 payload。 | Testing Library 驗證 Vue3 畫面行為 |

補充：本次已在 `HomeView.vue` 與 `UpdateLoansView.vue` 使用 `reactive` 管理放款總額折算狀態，包含目標幣別、匯率資料、讀取中與錯誤訊息；登入狀態仍由 Vuex 集中管理。



