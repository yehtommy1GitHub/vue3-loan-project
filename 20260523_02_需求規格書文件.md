```markdown
# 系統需求規格書 (SRS) - Vue3 用戶帳戶與放款管理系統

## 1. 業務需求與系統範圍 (Business Requirements & Scope)
本系統旨在建立一個具備適應性網頁設計（RWD）的 Vue3 前端應用，並搭配內建的測試 API Server。系統提供完整的用戶基礎功能（登入、註冊、個人資訊檢視），並進階支援核心業務「放款資訊」的動態維護管理（查詢、新增、修改、刪除）。

---

## 2. 業務流程 (Business Process Flow)

### 2.1 註冊與登入流程
1. **註冊**：新用戶於註冊頁輸入帳密（皆須 ≥ 8 碼）。送出後由 API Server 將資料動態寫入測試 JSON 檔案。新註冊用戶之放款資訊陣列預設為**空值**。
2. **登入**：用戶輸入帳密，系統進行前端長度預檢，通過後發送 Axios POST 請求。認證成功則載入該用戶資料並跳轉至首頁。

### 2.2 放款資訊維護流程
1. 用戶登入成功後，首頁向 API Server 請求該用戶的「基本資訊」與「放款帳號清單」。
2. 點選「放款資訊更新」後，系統導向獨立維護頁，讀取當前放款陣列。
3. 用戶於表格內進行編輯（增/刪/改），點選「存檔」時始觸發 API 變更並回寫 JSON 檔案；點選「返回」則放棄本次變更回到首頁。

---

## 3. 頁面功能規格 (Page Functional Specifications)

### 共通視覺與佈局規範 (Global UI UX Rules)
* **響應式設計 (RWD)**：所有頁面元件必須在畫面中**自動居中對齊**，並確保在行動端與桌機端皆有良好排版，降低或避免下方滾動條（Scroll Bar）的出現頻率。
* **按鈕排版原則**：在登入頁與註冊頁中，**「送出」按鈕統一配置於右側**，其餘按鈕（如：註冊、清除、返回）統一配置於左側。

---

### 3.1 帳密登入頁面 (Login Page)
* **頁面功能**：提供現有用戶驗證身份並進入系統的入口。
* **畫面元件與欄位檢核**：
  * **帳號輸入框**：必填。
  * **密碼輸入框**：必填，隱碼顯示。
  * **「註冊」按鈕**：位於**左側**。點選後跳轉至「帳密註冊頁面」。
  * **「送出」按鈕**：位於**右側**。
* **控制邏輯**：
  1. 點選「送出」時，**優先檢核**帳號與密碼之長度。任一欄位未輸入或**長度未達 8 碼**，則於畫面上提示對應錯誤，且**不觸發** JSON 電文發送。
  2. 通過檢核後，使用 Axios 發送 HTTPS POST 請求至 API Server。
  3. 驗證成功：引導用戶跳轉至「顯示使用者資訊首頁」。
  4. 驗證失敗：停留在登入頁，並明確提示「連線失敗」或「帳密錯誤」。

---

### 3.2 帳密註冊頁面 (Register Page)
* **頁面功能**：供新用戶建立帳號。
* **畫面元件與欄位檢核**：
  * **帳號輸入框**：必填。
  * **密碼輸入框**：必填，隱碼顯示。
  * **「返回」按鈕**：位於**左側**。點選後跳轉回「帳密登入頁面」。
  * **「清除」按鈕**：位於**左側**。點選後清空當前輸入的帳號與密碼。
  * **「送出」按鈕**：位於**右側**。
* **控制邏輯**：
  1. 點選「送出」時，進行**優先檢核**：確認帳號與密碼皆已輸入，且**兩者長度皆必須在 8 碼以上**。
  2. **動態錯誤提示機制**：若密碼長度未滿 8 碼，畫面上顯示錯誤提示，該提示必須在 **5 秒後自動消失**。
  3. 通過檢核後，使用 Axios 發送 HTTPS POST 請求進行註冊。註冊成功後，系統會自動將該用戶寫入後端測試檔案，並直接跳轉至「顯示使用者資訊首頁」（此時放款資訊陣列為空）。

---

### 3.3 顯示使用者資訊首頁 (Home / Profile Page)
* **頁面功能**：展示用戶的核心基本資訊與放款明細清單。頁面整體視覺調大，優化閱讀體驗。
* **畫面元件**：
  * **基本資訊區**：以中文標籤清晰顯示「使用者帳號」與「使用者名稱」。
  * **放款資訊表格**：若用戶擁有放款資料，應以 Table 形式展現。表格欄位依序為：
    1. **序列**（1, 2, 3... 動態遞增流水號）
    2. **放款帳號**
    3. **幣別**
    4. **當前現欠金額**
    5. **下期還款日期**
    6. **下期還款金額**
  * **「放款資訊更新」按鈕**：點選後開啟並跳轉至獨立的「放款資訊維護頁面」。
  * **「退出」按鈕**：點選後執行登出程序，清除執行期暫存，並跳轉回「帳密登入頁面」。

---

### 3.4 放款資訊維護頁面 (Loan Management Page)
* **頁面功能**：獨立提供用戶對其名下的放款資料進行異動與維護。
* **畫面元件**：
  * **動態維護表格**：完整列出該帳號當前的放款資訊陣列，每筆資料皆可直接在表格內進行編輯，並提供「新增一筆」與每筆後方的「刪除」功能。
  * **欄位格式防呆（檢核機制）**：
    * 放款帳號：必須為 13 碼純數字。
    * 幣別：必須為 3 碼純英文（如 TWD, USD）。
    * 下期還款日期：必須符合西元年月日格式 (`yyyymmdd`)。
  * **「返回」按鈕**：位於**左側**。點選後放棄所有未儲存的變更，直接回到首頁。
  * **「存檔」按鈕**：位於**右側**。點選後將整筆調整後（含新增、刪除）的陣列，透過 Axios API 發送至 Server端進行永續更新，存檔成功後回歸首頁。

---

## 4. API 暨資料電文規格 (API & Data Schema)

為符合主流開發規範，**傳輸電文與後端 JSON 檔案欄位統一採用英文命名**，前端畫面展示時再轉譯為中文。

### 4.1 核心資料模型（JSON 檔案切分結構）
後端內部資料管理主要切分為以下三隻獨立實體/檔案（以 `userId` 作為關聯鍵）：

#### (1) 使用者帳密資訊 (Auth Data)
```json
{
  "userId": "DEMO0002",
  "password": "password999"
}

#### (2) 使用者基本資訊 (Profile Data)
```json
{
  "userId": "tommy",
  "userName": "tommy使用者"
}

#### (3) 使用者放款帳號清單 (Loan Data)
```json
{
  "userId": "tommy",
  "loanList": [
    {
      "loanAccount": "1234567890123",
      "currency": "TWD",
      "currentBalance": 500000,
      "nextPaymentDate": "20260630",
      "nextPaymentAmount": 15000
    }
  ]
}

### 4.2 測試案例初始資料定義 (Test Cases)
| 測試帳號 <br>`(userId)` | 密碼 <br>`(password)` | 使用者姓名 <br>`(userName)` | 放款資訊陣列 <br>`(loanList)` 狀態與明細範例 |
| :--- | :--- | :--- | :--- |
| **DEMO0002** | `SamplePass456!` <br>*(符合 $\ge$ 8 碼)* | 示範使用者二 | **包含至少 3 筆符合規格之測試資料**：<br>1. 帳號: `1111222233334` / TWD / 1,500,000 / 20260715 / 25,000<br>2. 帳號: `5555666677778` / USD / 40,000 / 20260820 / 1,200<br>3. 帳號: `9999000011112` / TWD / 350,000 / 20260730 / 8,000 |
| **tommy** | `DEMO00014` <br>*(符合 $\ge$ 8 碼)* | tommy使用者 | **包含至少 3 筆符合規格之測試資料**：<br>1. 帳號: `1234567890123` / TWD / 500,000 / 20260630 / 15,000<br>2. 帳號: `2345678901234` / EUR / 12,000 / 20260910 / 500<br>3. 帳號: `3456789012345` / TWD / 880,000 / 20260625 / 18,500 |
| **新註冊用戶** | 用戶自訂 <br>*(前端強制檢核 $\ge$ 8 碼)* | 新用戶 <br>*(或同帳號名稱)* | **預設為空陣列 `[]`**<br>（登入首頁時不顯示表格，點選「放款資訊更新」後可動態新增自訂放款資料） |