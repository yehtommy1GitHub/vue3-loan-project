// 相容舊路徑：正式 API 自建模組已移到 src/api/config。
export { backendApiConfig, buildFullApiUrl, resolveBackendApiEndpoint } from '../api/config/backendApiConfig';
export type { BackendApiEndpointName } from '../api/config/backendApiConfig';
