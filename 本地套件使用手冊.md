# API 本地套件使用手冊

最後更新時間：2026/06/10 21:26:16

## 1. 套件清單

| 套件 | 套件名稱 | 用途 | 安裝後主要入口 |
|---|---|---|---|
| API SDK | `@vue3-invoice/api-sdk` | 建立通用 API Client、管理端點、統一 request、錯誤處理與業務成功判斷。 | `createApiSdk` |
| 發票平台 API | `@vue3-invoice/platform-api` | 提供登入、註冊、使用者、發票放款、異動紀錄與匯率 API。 | `createInvoicePlatformApi` |

發票平台 API 已將 API SDK 核心打包在套件內，因此只使用發票平台功能時，可單獨安裝 `@vue3-invoice/platform-api`。需要自行建立其他業務 API 時，再安裝通用 API SDK。

## 2. 建置與打包

| 指令 | 用途 |
|---|---|
| `npm run test:packages` | 執行兩個本地套件的單元測試。 |
| `npm run build:api-sdk` | 建置 API SDK JavaScript 與 TypeScript declaration。 |
| `npm run build:platform-api` | 建置發票平台 API JavaScript 與 TypeScript declaration。 |
| `npm run build:packages` | 依序建置兩個套件。 |
| `npm run pack:packages` | 建置後執行 `npm pack`，在 `local-packages/` 產生 `.tgz`。 |

預期產物：

```text
local-packages/
  vue3-invoice-api-sdk-1.0.0.tgz
  vue3-invoice-platform-api-1.0.0.tgz
```

`.tgz` 與各套件 `dist/` 都是可重新產生的建置產物，不得上傳 GitHub。

## 3. API SDK 安裝與使用

在其他 Vue3 或 TypeScript 專案中執行：

```bash
npm install C:\Users\tommy\Desktop\vue3-loan-project\local-packages\vue3-invoice-api-sdk-1.0.0.tgz
```

建立 API SDK：

```ts
import { assertApiSuccess, createApiSdk } from '@vue3-invoice/api-sdk';

const apiSdk = createApiSdk({
  baseURL: 'http://127.0.0.1:8080',
  timeout: 8000,
  endpoints: {
    fetchUser: {
      method: 'GET',
      path: ({ account }) => `/users/${encodeURIComponent(String(account ?? ''))}`
    },
    updateUser: {
      method: 'PUT',
      path: ({ account }) => `/users/${encodeURIComponent(String(account ?? ''))}`
    }
  }
});

const response = await apiSdk.request<{ success: boolean; user?: unknown }>('fetchUser', {
  params: { account: 'DEMO0001' }
});

assertApiSuccess(response);
```

### 注入既有 Axios Client

```ts
import axios from 'axios';
import { createApiSdk } from '@vue3-invoice/api-sdk';

const client = axios.create({
  baseURL: 'http://127.0.0.1:8080'
});

client.interceptors.request.use((config) => {
  config.headers.Authorization = 'Bearer SAFE_EXAMPLE_TOKEN';
  return config;
});

const apiSdk = createApiSdk({
  baseURL: 'http://127.0.0.1:8080',
  client,
  endpoints: {
    fetchUser: {
      method: 'GET',
      path: '/users/DEMO0001'
    }
  }
});
```

## 4. 發票平台 API 安裝與使用

在其他 Vue3 或 TypeScript 專案中執行：

```bash
npm install C:\Users\tommy\Desktop\vue3-loan-project\local-packages\vue3-invoice-platform-api-1.0.0.tgz
```

建立發票平台 API：

```ts
import { createInvoicePlatformApi } from '@vue3-invoice/platform-api';

export const invoiceApi = createInvoicePlatformApi({
  baseURL: 'http://127.0.0.1:8080',
  timeout: 8000
});
```

業務 API 使用範例：

```ts
const user = await invoiceApi.login('DEMO0001', 'DemoPass123!');

const registeredUser = await invoiceApi.register(
  'DEMO0003',
  'DemoPass789!',
  '安全範例使用者'
);

const latestUser = await invoiceApi.fetchUser('DEMO0001');
const rates = await invoiceApi.fetchExchangeRates();
const logs = await invoiceApi.fetchLoanChangeLogs('DEMO0001');

await invoiceApi.updateLoans('DEMO0001', [
  {
    loanAccount: '9000000000001',
    currency: 'TWD',
    currentOutstandingAmount: 1000,
    nextPaymentDate: '20260701',
    nextPaymentAmount: 100
  }
]);
```

### 覆寫特定端點

```ts
const invoiceApi = createInvoicePlatformApi({
  baseURL: 'https://backend.example.test',
  endpoints: {
    fetchUser: {
      method: 'GET',
      path: ({ account }) => `/v2/members/${encodeURIComponent(account ?? '')}`
    }
  }
});
```

### 使用 TypeScript 型別

```ts
import type { Loan, UserProfile } from '@vue3-invoice/platform-api';

const loans: Loan[] = [];
const user: UserProfile = await invoiceApi.fetchUser('DEMO0001');
```

## 5. 套件更新流程

| 序列 | 操作 | 說明 |
|---|---|---|
| 1 | 修改套件 source 與測試 | API SDK 位於 `packages/api-sdk/src`；發票平台 API 位於 `packages/invoice-platform-api/src`。 |
| 2 | 調整版本 | 修改對應套件 `package.json` 的 `version`。 |
| 3 | 執行套件測試 | `npm run test:packages`。 |
| 4 | 執行打包 | `npm run pack:packages`。 |
| 5 | 使用端重新安裝 | `npm install <新版本 tgz 路徑>`。 |
| 6 | 使用端驗證 | 執行使用端的 type-check、test 與 build。 |

## 6. 常見問題

| 問題 | 原因 | 處理方式 |
|---|---|---|
| 使用端仍讀到舊功能 | `.tgz` 版本未更新或 npm 使用舊安裝內容。 | 提升套件版本後重新 `npm pack` 與 `npm install`。 |
| TypeScript 找不到型別 | 套件未完成 build，或 `.tgz` 未包含 `dist/*.d.ts`。 | 執行 `npm run build:packages`，再重新打包。 |
| API URL 錯誤 | 使用端 `baseURL` 或覆寫端點設定錯誤。 | 檢查 `createApiSdk` 或 `createInvoicePlatformApi` 設定。 |
| Token 未帶入 | 未注入含 interceptor 的 Axios instance。 | 透過 `client` 傳入使用端既有 Axios instance。 |
| 安裝後修改原始碼未生效 | `.tgz` 是固定快照，不會即時同步 source。 | 重新 build、pack、install。 |

## 7. 本次交付驗證

| 驗證項目 | 結果 |
|---|---|
| API SDK 單元測試 | 4 個案例通過 |
| 發票平台 API 單元測試 | 5 個案例通過 |
| 套件 build | JavaScript bundle 與 TypeScript declaration 建置成功 |
| npm pack | 兩個 `1.0.0` `.tgz` 產生成功 |
| 獨立安裝 smoke-test | 從 `.tgz` 安裝後，可成功 import、建立 API 實例、呼叫並通過 TypeScript declaration 檢查 |
| 完整專案測試 | 11 個測試檔、46 個測試案例通過 |
| 原 Vue3 專案 build | 成功 |
