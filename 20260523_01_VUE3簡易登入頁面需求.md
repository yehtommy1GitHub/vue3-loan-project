幫我生成一個VUE3專案，需求如下：

一、3個頁面
1.帳密登入頁面
2.顯示使用者資訊首頁
3.帳密註冊頁面

二、帳密登入頁面，有以下元件與功能
1.帳號：供使用者輸入帳號
2.密碼：供使用者輸入密碼
3.送出：
  (1)點選會檢查帳密是否有輸入，無則顯示帳密未輸入
  (2)若有輸入則呼叫HTTPS API POST，上行電文為JSON
  (3)成功收到下行JSON成功資訊，則跳轉顯示使用者資訊首頁
  (4)失敗則回到帳密登入頁面，顯示連線失敗或帳密錯誤
4.註冊：跳轉到帳密註冊頁面

三、顯示使用者資訊首頁，
1.文字，用JSON顯示使用者帳號、使用者姓名、使用者個人資訊
2.退出，登出回到帳密登入頁面

四、帳密註冊頁面，有以下元件與功能
1.帳號：供使用者輸入帳號
2.密碼：供使用者輸入密碼，需有長度8碼以上檢核機制
3.送出：
  (1)點選會檢查帳密是否有輸入，無則顯示帳密未輸入
  (2)若有輸入則呼叫HTTPS API POST，上行電文為JSON
  (3)成功收到下行JSON成功資訊，則跳轉顯示使用者資訊首頁
4.清除：清除使用者填寫之帳號、密碼

===============================================================
五、精進1
1.POST API須用AXIOS
2.透過VUE3建立一個API SERVER，方便測試登入、註冊、首頁JSON
3.密碼長度需為 8 碼以上要5秒後自動消失
4.除了node_modules、dist，其他檔案文件幫我另外整理成LIST程式清單並存成MD格式，欄位如下： 
  (1)路徑
  (2)檔名
  (3)檔案用途
5.帳密註冊頁面，新增返回
6.帳密註冊頁面、帳密登入頁面，送出統一在右邊，其他按鈕統一在左邊

六、精進2
1.新增測試案例
  (1)帳號：DEMO0002
  (2)密碼：9999999
2.調整DEMO0002、tommy下行電文JSON，欄位如下：
  (1)帳號
  (2)使用者姓名
  (3)放款資訊陣列(至少3筆)
    (a)放款帳號(13碼數字)
    (b)幣別(三碼英文)
    (c)當前現欠金額
    (d)下期還款日期(西元年月日yyymmdd)
    (e)下期還款金額
三、如果是新註冊用戶，陣列會是空值

七、精進3
1.精進顯示使用者資訊首頁，調整如下：
  (1)使用者帳號：tommy
  (2)使用者名稱：tommy使用者
  (3)陣列用表格表示，並加上序列1、2、3...等
2.註冊與測試下行電文資料，可自動存成一個測試案例下行json檔案，供api server讀取使用， 方便測試

八、精進4
精進以下功能
1.json欄位改成英文，符合主流json命名規則，畫面依舊顯示中文字
2.json切成3隻
  (1)一隻是使用者帳密資訊，只有使用者帳號、使用者密碼
  (2)一隻是使用者基本資訊，只有使用者帳號、使用者姓名
  (3)一隻是使用者放款帳號清單，只有使用者帳號、放款資訊陣列
3.放款資訊陣列調整如下：
  (1)放款帳號(13碼數字)
  (2)幣別(三碼英文)
  (3)當前現欠金額
  (4)下期還款日期(西元年月日yyymmdd)
  (5)下期還款金額

八、精進5
1.首頁新增「放款資訊更新」按鈕
  (1)點選後會開啟另外一個新頁面
  (2)把該帳號當前「放款資訊」全數顯示，並可透過表格進行調整、新增、刪除
  (3)點選存檔後才會更新資料
  (4)點選返回則會到首頁
2.登入、註冊頁面優先檢核帳號、密碼長度，確認帳號、密碼至少8長以上，才觸發JSON電文
3.調整帳密測試案例，帳號密碼至少8長以上

八、精進6
1.畫面要RWD自動置中
2.使用者資訊首頁可以大一點，降低下方BAR出現頻率

九、精進7
1.依據此專案撰寫MD格式之系統分析設計規格書，檔案名稱為「系統分析設計規格書.md」
2.內容包括如下，但可依需求新增各種頁簽：
  (1)版本記錄
  (2)業務需求與系統範圍
  (3)業務流程
  (4)頁面功能規格
  (5)API 暨資料電文規格
  (6)驗收測試案例
  (7)關聯圖
3.請將主要程式碼逐行補上完整中文註解

十、精進8
1.幣別用下拉式選單，可選USD、TWD、JPY、SGD、EUR...等
2.金額欄位顯示使用國際標準格式，每三碼就會有一個逗點，並允許小數點後6碼
3.下期還款日期用日曆方式供使用者點選

十一、精進9
精進系統功能，新增放款資訊異動紀錄功能，內容如下： 
1.於放款資訊下方新增異動紀錄表格，方便使用者快速查看過往紀錄，採日期倒敘排列
2.當有新增異動放款資訊時，就會依據異動內容自動產生以下資訊
  (1)異動時間(YYYY/MM/DD HH:MM:SS)
  (2)異動者姓名
  (3)異動項目
  (4)異動內容
3.新增全新JSON API

十二、精進9
有BUG與需精進項目：
1.放款資訊異動紀錄不完整，要完整顯示所有新增異動欄位資訊
  (1)可用JSON陣列表示
  (2)欄位應該是異動項目(新增、修改、刪除)，異動資料
  (3)請改成表格顯示，然後顯示修改前、修改後
2.放款資訊更新，所有資料都是必輸入，少一個就建檔失敗，放款帳號限定13長以上
3.幣別下拉式選單顯示不清除，需放大

十三、精進10
1.放款資訊更新頁面，限制放款帳號只要新增成功就不能調整，若要調整帳號只能刪除重新新增
2.異動資料表格主體必顯示放款帳號，才能讓使用者知道哪個放款帳號被異動

十四、精進11
1.帳密註冊頁面，新增使用者名稱，透過這邊新增使用者名稱，而非出現預設名稱
2.使用者名稱限定2長以上，如果不足2長就紅字提醒「使用者名稱必須大於2長」等字句

十四、精進12
1.放款資訊更新頁面，放款帳號改成只能輸入13碼數字，且不能重複
2.放款資訊更新頁面，當前現欠金額必須大於等於下期還款金額
3.「程式清單.md」新增使用VUE3技術欄位，將有用此技術的檔案更新於程式清單內
axios、vuex、props、emit、router-link、router-view、ref、reactive、v-for、onMounted、computed、vue-next-select、vee-validate、v-model、v-on、v-bind
4.放款資訊更新－點選幣別無法看到所有幣別，像卡住一樣無法點選，應該是太小導致，再請修正

十四、精進13
1.放款資訊更新頁面、放款資訊頁面，新增總限欠金額、總還款金額資訊，必須折算匯率後加總，方便使用者快速知道總金額
2.總限欠金額、總還款金額，旁邊新增幣別下拉式選單，可全數快速折算指定幣別
3.匯率部分另外寫一隻API模擬呼叫匯率
4.測試資料有髒資料(放款帳號大於13長)，請修正

十五、精進14
1.放款資訊更新頁面、放款資訊頁面，總幣別下拉式選單旁邊，新增一按鈕「當前匯率資訊」，用來顯示目前選擇的總匯率對應其他匯率比值，方便使用者計算與驗證
2.點選「當前匯率資訊」後，開啟新分頁並進入「當前匯率資訊頁面」
3.當前匯率資訊頁面，資訊如下：
(1)本位幣幣別下拉式選單
(2)匯率更新時間資訊
(3)各幣別匯率比值表格
(4)預設本位幣為TWD新臺幣